database:
  url: "postgres://postgres:indexbrain@localhost/testdb"
  poll_interval_secs: 60
  required_extensions:
    - pg_stat_statements
    - hypog

collector:
  fetch_query_text: true
  max_queries: 1000

